'use strict'

let numbers = [19, 17, 29];

if (numbers[0] > numbers[1]) {
    if (numbers[0] > numbers[2]) {
        console.log(numbers[0]);
    } else {
        console.log(numbers[2]);
    }
} else {
    if (numbers[1] > numbers[2]) {
        console.log(numbers[1]);
    } else {
        console.log(numebrs[2]);
    }
}